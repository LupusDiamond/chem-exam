/// Core App File

// Packages
const express = require("express");
const path = require("path");

const { e_app } = require("./utils/express/index");

const dir = __dirname;

module.exports = {
  dir
};
